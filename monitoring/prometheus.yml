# Prometheus scrape config for PRAGMA-G (PLAN.md Week 7)

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: pragma-g-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
