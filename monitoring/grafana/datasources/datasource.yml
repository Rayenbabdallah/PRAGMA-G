# Grafana datasource provisioning for PRAGMA-G (PLAN.md Week 7)

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
